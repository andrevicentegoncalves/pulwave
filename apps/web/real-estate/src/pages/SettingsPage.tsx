/**
 * SettingsPage - Real Estate App (Thin Wrapper)
 * Imports from the settings package
 */
import { SettingsPage as Settings } from '@pulwave/features-settings';

export const SettingsPage = () => {
    return <Settings />;
};

export default SettingsPage;
