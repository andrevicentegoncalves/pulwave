import { cva, type VariantProps } from 'class-variance-authority';

export const commandVariants = cva('command', {
    variants: {},
    defaultVariants: {}
});

export const commandDialogVariants = cva('command-dialog', {
    variants: {},
    defaultVariants: {}
});

export const commandInputWrapperVariants = cva('command__input-wrapper', {
    variants: {},
    defaultVariants: {}
});

export const commandListVariants = cva('command__list', {
    variants: {},
    defaultVariants: {}
});

export const commandEmptyVariants = cva('command__empty', {
    variants: {},
    defaultVariants: {}
});

export const commandGroupVariants = cva('command__group', {
    variants: {},
    defaultVariants: {}
});

export const commandItemVariants = cva('command__item', {
    variants: {},
    defaultVariants: {}
});

export const commandShortcutVariants = cva('command__shortcut', {
    variants: {},
    defaultVariants: {}
});

export const commandSeparatorVariants = cva('command__separator', {
    variants: {},
    defaultVariants: {}
});
